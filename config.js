window.CROSSCART_CONFIG = {
  // Keep this empty for the built-in preview catalog. Later, point it at the
  // private API service; never put Amazon credentials in this public file.
  apiBaseUrl: "",
};
